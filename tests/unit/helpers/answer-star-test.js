import { answerStar } from 'questions-answers/helpers/answer-star';
import { module, test } from 'qunit';

module('Unit | Helper | answer star');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = answerStar([42]);
  assert.ok(result);
});
